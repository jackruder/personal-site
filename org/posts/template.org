# Required frontmatter keywords
#+TITLE: Template Post Title
#+DESCRIPTION: Replace this description with a 20-200 character summary for RSS and social previews.
#+DATE: 2026-05-14

# Optional keywords
#+UPDATED: 2026-05-14
#+TAGS: example, math org astro
#+DRAFT: t
#+OG_IMAGE: /TODO/custom-og-image.png

* Section heading

Use this file as a starting point for new posts. The title above becomes the page title, so do not repeat it in the body.

Inline math looks like \(a^2 + b^2 = c^2\), and display math looks like:

\[
\int_0^1 x^2 \, dx = \frac{1}{3}
\]

#+BEGIN_SRC python
values = [n * n for n in range(4)]
print(values)
#+END_SRC

#+BEGIN_EXPORT mdx
import Callout from '../../components/ui/Callout.astro';

<Callout type="note">
  This export block shows how to embed a reusable Astro component inside a post.
</Callout>
#+END_EXPORT

#+BEGIN_EXPORT mdx
import PlaceholderDemo from '../../components/demos/PlaceholderDemo.tsx';

<PlaceholderDemo client:visible />
#+END_EXPORT

Here is a footnote reference.[fn:1]

[[file:../assets/hello-world/placeholder.png]]

[fn:1] Replace this note with a real citation or aside when you publish.
