name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install pandoc
        run: |
          sudo apt-get update
          sudo apt-get install --yes pandoc

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build site
        run: make build

      # - name: Deploy with rsync
      #   run: |
      #     # TODO: add server details, destination path, and SSH key setup before enabling deploys.
      #     rsync -avz dist/ user@example.com:/var/www/personal-site/
